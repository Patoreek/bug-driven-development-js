# Hint 1 — Approach

Instead of sorting (which inherently requires O(n log n) comparisons), think about what property defines an anagram.

Two strings are anagrams if and only if they have the **exact same character frequencies**. How can you count and compare character frequencies in linear time?
