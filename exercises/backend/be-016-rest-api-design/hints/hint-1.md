# Hint 1 (Mild)

Look at the route URLs -- they all contain verbs like "get", "fetch", "create", "update", "remove". In REST, the URL should identify a resource (noun), and the HTTP method should express the action. What should these URLs look like?
