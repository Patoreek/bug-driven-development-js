# Hint 1 (Mild)

After `login` succeeds, compare the returned session ID with the one passed in. Are they the same? If an attacker gave the victim a session cookie with a known ID before the victim logged in, could the attacker now use that same ID to access the authenticated session?
