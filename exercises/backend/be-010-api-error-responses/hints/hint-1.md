# Hint 1 (Mild)

Look at the error responses across all four handler functions. Count how many different keys are used for the error message (`error`, `message`, `msg`). Also check the HTTP status codes -- does a "not found" response always return 404?
