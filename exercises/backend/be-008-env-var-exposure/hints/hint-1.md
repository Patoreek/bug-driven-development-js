# Hint 1 (Mild)

Look at the `getHealthCheck` function. It returns the full config object with all fields. Which of those fields would be dangerous if an attacker saw them? Think about passwords, API keys, and secrets.
