# Hint 2 (Medium)

The `escapeHtml` function needs to replace five characters with their HTML entity equivalents:
- `&` -> `&amp;`
- `<` -> `&lt;`
- `>` -> `&gt;`
- `"` -> `&quot;`
- `'` -> `&#39;`

Be careful about the order -- which character should be replaced first to avoid double-encoding?
