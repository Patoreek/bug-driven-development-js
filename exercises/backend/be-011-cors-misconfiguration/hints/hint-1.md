# Hint 1 (Mild)

The CORS spec says you cannot use `Access-Control-Allow-Origin: *` together with `Access-Control-Allow-Credentials: true`. Browsers will reject the response. What should the origin header be set to instead?
