# Hint 1 (Mild)

The handler successfully verifies that the user is logged in. But being logged in doesn't mean you can do anything. What property on the `user` object would tell you whether they're allowed to delete articles?

Think about the difference between *authentication* (who are you?) and *authorization* (what can you do?).
