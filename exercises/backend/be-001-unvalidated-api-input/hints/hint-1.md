# Hint 1 (Mild)

TypeScript's `as` keyword only affects the compiler — it does nothing at runtime. What happens when someone sends `{ name: "", price: "hello" }` to your handler?

Think about what tool the project already has installed that could validate data at runtime.
