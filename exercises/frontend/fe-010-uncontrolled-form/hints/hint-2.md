# Hint 2 (Medium)

You need to convert the inputs to "controlled components" by adding `useState` for each field and binding `value` and `onChange`. Then in `handleReset`, set all state values back to empty strings along with the submitted flag.
