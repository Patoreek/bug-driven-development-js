# Hint 1 (Mild)

Look at what's in the `useEffect` dependency array. Is it a primitive value (string, number, boolean) or a reference type (object, array, function)? How does JavaScript compare those?
