# Hint 1 (Mild)

Think about what value `count` has when the `setTimeout` callback is created versus when it actually fires. JavaScript closures capture variables at the time the function is created, not when it executes.
