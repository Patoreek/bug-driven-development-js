# Hint 2 (Medium)

React provides a hook specifically for caching expensive computations: `useMemo`. It takes a function and a dependency array, and only re-runs the function when a dependency changes.
