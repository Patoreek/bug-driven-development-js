# Hint 1 (Mild)

Look at the `EventName` type. It concatenates `"on"` with the event string directly. What built-in TypeScript intrinsic type could you use inside a template literal to transform the case of a string type?

For `CSSProperty`, compare the output carefully. The hyphen is inserted correctly, but what case is the letter after it?

For `Split`, look at the order of elements in the tuple being constructed. Is `Head` placed before or after the recursive result?
