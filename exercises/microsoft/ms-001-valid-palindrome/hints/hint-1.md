# Hint 1 - Approach

The problem says "considering only alphanumeric characters and ignoring cases." The current solution doesn't do either of those things.

Think about what needs to happen before you can compare characters:
- What characters should be skipped entirely?
- Should 'A' and 'a' be treated as the same character?
