# Hint 1 (Mild)

Look at the test expectations carefully. Each function is expected to return objects with a specific set of keys -- not the full user. `getUsersForList` should only have 3 fields, `getUsersForCard` should only have 4 fields.
