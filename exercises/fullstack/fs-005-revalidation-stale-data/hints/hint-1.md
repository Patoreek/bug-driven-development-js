# Hint 1 (Mild)

Look at what `getBookmarks` does when `cachedBookmarks` is not null. Now look at what `addBookmark` and `deleteBookmark` do (or rather, don't do) to `cachedBookmarks` after modifying the data.
