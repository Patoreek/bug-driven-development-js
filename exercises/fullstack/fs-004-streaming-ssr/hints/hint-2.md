# Hint 2 (Medium)

React's `<Suspense>` component lets you show a fallback (like a loading skeleton) while an async child component resolves. Move the slow data fetches into their own async components, then wrap them in `<Suspense>`.
