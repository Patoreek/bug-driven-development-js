# Hint 1

Try this in a console:

```js
const arr = [3, 1, 2];
const sorted = arr.sort((a, b) => a - b);
console.log(arr === sorted); // What does this print?
```

Does `.sort()` return a new array, or the same one?
