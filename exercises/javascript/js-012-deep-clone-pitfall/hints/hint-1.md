# Hint 1 -- Mild

Think about what the spread operator (`{...obj}`) actually copies. Does it copy nested objects, or just references to them? Try modifying a nested property on the "clone" and check whether the original changes too.
