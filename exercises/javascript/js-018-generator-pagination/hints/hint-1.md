# Hint 1 -- Mild

Look at what `paginatedFetch` yields. When `page.items` is `[1, 2, 3]`, what does `yield page.items` produce? Is it one value or three? What keyword would you use to yield each element of an array individually?

Also check the loop condition -- when should the fetcher stop making API calls?
