# Hint 1

What happens when you call `.reduce()` on an empty array? Try it in a console:

```js
[].reduce((acc, item) => acc + item)
```

Also consider: what is the type of `acc` on the very first iteration?
