# Hint 1

What does `.map()` return when the callback itself returns an array? Try logging the intermediate result of `posts.map(post => post.tags)` — is it a flat array of strings, or an array of arrays?
