# Hint 2

JavaScript's `.sort()` without arguments converts elements to **strings** and sorts them lexicographically (dictionary order). The string `"10"` comes before `"2"` because `"1"` < `"2"`. You need to provide a **comparison function** that compares values numerically.
