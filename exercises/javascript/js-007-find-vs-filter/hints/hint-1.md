# Hint 1

What does `.filter()[0]` return when no elements match? And what does the `?? null` at the end do to that value? Think about what callers expect when a user is not found.

Also consider: does `.filter()` stop iterating when it finds the first match?
