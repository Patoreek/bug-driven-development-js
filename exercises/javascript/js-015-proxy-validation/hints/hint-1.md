# Hint 1 -- Mild

Look at the `set` trap. In strict mode (which TypeScript always uses), what must a `set` trap return to indicate the operation succeeded? What happens if it returns `undefined`?

Also look at the `get` trap -- is it using the best practice for accessing the target's properties?
