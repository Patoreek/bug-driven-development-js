# Hint 1

Think about what `Boolean(value)` returns for different values. Try evaluating `Boolean(0)`, `Boolean("")`, and `Boolean(false)` in your head or in a console. Are all of those values you want to remove?
